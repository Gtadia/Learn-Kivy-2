#:kivy 2.1.0

<Button>
    # 1
    # font_size: 50 
    background_color: ''
    background_color: (1, 0, 0, 1)

<TextInput>
    background_color: (1, 1, 0, 1)

<Label>
    font_size: 30   # 2


<MyLayout>
    BoxLayout:
        size: root.width, root.height
        orientation: "vertical"

        spacing: 10
        padding: 10

        Label:
            text: "Name"
        TextInput:
            multiline: False
        
        Label: 
            text: "Favorite Pizza"
        TextInput:
            multiline: False
 

        Button:
            text: "Button"
        Button: 
            text: "Clear"
            background_color: (0, 1, 0, 1)      # 3 


# 1
# We can apply certain attributes for every button (or any other widget, including custom widgets like "MyWidget") to INHERIT

# 2
# We have to be careful with label font_size because anything with "text", which includes buttons, are going to inherit label's
# font_size.

# 3
# You can override the default attributes that it inherits